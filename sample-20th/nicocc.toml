# ---------------------------------------------------------
#   ログイン情報
# ---------------------------------------------------------

[user]

# ニコニコのログインアカウント
mail = "xxxxxxxxx@sample.com"

# ニコニコのログインパスワード
password = "xxxxxxxxxx"


# ---------------------------------------------------------
#   集計パラメータ
# ---------------------------------------------------------

[counter]

# 集計を開始する時刻
start = "2018-02-09 21:00:00"

# 集計を終了する時刻
end = "2018-02-26 21:00:00"

# 集計対象となる動画を登録したマイリストのID
# ここで指定したマイリストから動画IDを取得し、videos.txt を生成する
# videos.txt が既に存在する場合はスキップされる
mylist = [ 60232979 ]

# 入出力ファイルの文字コード
encoding = "utf-8"


# ---------------------------------------------------------
#   HTTP接続パラメータ
# ---------------------------------------------------------

[http]

# 連続でサーバーに接続する間隔(ミリ秒)
interval = 1000

# サーバーエラー発生時の待機時間(ミリ秒)
server_error_interval = 30000

# 通信失敗時の再試行回数
retry = 4


# ---------------------------------------------------------
#   ログ
# ---------------------------------------------------------

[logging]

# FATAL, ERROR, WARNING, INFO, DEBUG
level = "INFO"
format = "%(asctime)s [%(levelname)s] %(name)s: %(message)s"
